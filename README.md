# InjectCCDC
This inject does Backs up firewall rules,  Enables firewall on all profiles &amp; sets DefaultInbound = Block,  Adds explicit Allow for TCP 80/443 (all profiles),  Adds explicit Block for all other TCP (1–79, 81–442, 444–65535) and all UDP,  Disables any other inbound allow rules.
